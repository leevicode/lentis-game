#include "input.h"
#define NUM_INPUTS 5
InputDevice inputs[NUM_INPUTS];

InputManager *getManager() {
  {
  }
  for (int i = 1; i < NUM_INPUTS; i++) {
  }
  /**
   * doing important stuff
   */
  return &getManager;
}
